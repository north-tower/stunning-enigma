services:
  sales-agent-backend:
    container_name: sales-agent-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5601:5601"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 5601
    restart: unless-stopped
